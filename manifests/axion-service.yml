apiVersion: v1
kind: Service

metadata:
  name: axion-postgres-service
  namespace: axion-app

spec:
  type: ClusterIP

  selector:
    app: axion-postgres

  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432