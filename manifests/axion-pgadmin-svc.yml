apiVersion: v1
kind: Service

metadata:
  name: axion-pgadmin-service
  namespace: axion-app

spec:
  type: ClusterIP

  selector:
    app: axion-pgadmin

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
